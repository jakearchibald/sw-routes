{
  "name": "sw-routes",
  "version": "1.0.0",
  "description": "A basic router for service workers",
  "main": "index.js",
  "scripts": {
    "build-test": "(cd test && rollup --config)",
    "build": "del lib dist && tsc && rollup --config"
  },
  "author": "Jake Archibald",
  "license": "ISC",
  "dependencies": {
    "path-to-regexp": "^2.2.1"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "rollup": "^0.45.2",
    "rollup-plugin-babili": "^3.1.0",
    "rollup-plugin-commonjs": "^8.1.0",
    "rollup-plugin-node-resolve": "^3.0.0",
    "typescript": "^2.9.2"
  }
}
