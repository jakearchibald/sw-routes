import { AnyHandler } from '../handler-types';
/**
 * Handler that waits a given number of milliseconds.
 *
 * @param ms Milliseconds to wait
 */
export default function wait(ms: number): AnyHandler;
